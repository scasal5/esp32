#pragma once

#include "shell.h"

/*
 * Ajustes: informacion del dispositivo, solo lectura. Todo lo que muestra sale
 * de APIs que ya existen; no hay nada guardado ni configurable todavia.
 */
extern const os_app_t app_ajustes;
