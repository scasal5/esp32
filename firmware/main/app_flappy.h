#pragma once

#include "shell.h"

/*
 * Flappy Bird: clon jugable en el lanzador (id "flappy").
 *
 * Graficos: placeholders con las mismas medidas que los sprites clasicos
 * (ave 34x24, tubo 52 de ancho, suelo 112 de alto) para poder sustituirlos
 * por assets OSS con licencia cuando esten listos. No se usan assets
 * propietarios.
 */
extern const os_app_t app_flappy;
